# Docker: Build, Run, Push

## Why Docker?

Docker ensures the webapp runs the same way everywhere:
local laptop, CI pipeline, Kubernetes cluster, cloud.

---

## Dockerfile

Explain your Dockerfile here briefly.

---

## Build the image locally

```bash
docker build -t motoglimplse-wildography:latest .
